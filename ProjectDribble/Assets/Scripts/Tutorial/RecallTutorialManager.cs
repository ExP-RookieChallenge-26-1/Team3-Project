using UnityEngine;

// Deprecated: recall tutorial display is now owned by TutorialManager + TutorialUI.
// This component remains only so older scene references keep loading without duplicate popups.
public class RecallTutorialManager : MonoBehaviour
{
}
